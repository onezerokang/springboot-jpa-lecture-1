# 스프링부트에서 별도의 설정이 없으면 memeory 모드로 돌려버린다!!!
spring:
#  datasource:
#    url: jdbc:h2:mem:test # 테스트 환경에서 in-memory 모드로 db를 띄운다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        #        show_sql: true
#        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6